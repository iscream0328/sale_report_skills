interface:
  display_name: "Build Portfolio Metadata"
  short_description: "Build and incrementally update portfolio metadata from portfolio/."
  default_prompt: "Build or update metadata and an HTML viewer for the images in the default portfolio/ folder."
